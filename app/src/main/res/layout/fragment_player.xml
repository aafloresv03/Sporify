<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/playerRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_player_default">

    <!-- Carrusel superior de álbumes -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/carouselAlbums"
        android:layout_width="match_parent"
        android:layout_height="260dp"
        android:paddingHorizontal="90dp"
        android:clipToPadding="false"
        android:clipChildren="false"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Imagen del álbum actual -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/albumArt"
        android:layout_width="260dp"
        android:layout_height="260dp"
        android:layout_marginTop="24dp"
        android:scaleType="centerCrop"
        app:shapeAppearanceOverlay="@style/Shape.Rounded.Large"
        app:layout_constraintTop_toBottomOf="@id/carouselAlbums"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Barra de progreso -->
    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/progressBar"
        android:layout_width="300dp"
        android:layout_height="6dp"
        android:layout_marginTop="40dp"
        android:max="100"
        android:progress="30"
        app:indicatorColor="@color/colorPrimary"
        app:trackColor="@color/colorSurface"
        app:trackThickness="6dp"
        app:layout_constraintTop_toBottomOf="@id/albumArt"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


    <!-- Gradiente inferior para suavizar el BottomNav -->
    <View
        android:id="@+id/bottomGradient"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/player_gradient"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- Controles -->
    <LinearLayout
        android:id="@+id/playerControls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="28dp"
        app:layout_constraintTop_toBottomOf="@id/progressBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/btnPrev"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:padding="8dp"
            android:src="@drawable/retroceder_icon"
            app:tint="@android:color/white"/>

        <ImageView
            android:id="@+id/btnPlay"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:padding="12dp"
            android:layout_marginHorizontal="40dp"
            android:src="@drawable/play_icon"
            app:tint="@android:color/white"/>

        <ImageView
            android:id="@+id/btnNext"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:padding="8dp"
            android:src="@drawable/next_icon"
            app:tint="@android:color/white"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
